name: 'Build and deploy - dev'


on:
  workflow_dispatch:

env:
  CARGO_TERM_COLOR: always

jobs:
  upload-docker-dev:
    runs-on: ubuntu-latest
    name: Upload docker dev images
    env:
      IMAGE_TAG: ${{ github.run_number }}
    outputs:
      image_tag: ${{ env.IMAGE_TAG }}
    steps: 
      - name: Check out code
        uses: actions/checkout@v3

      - name: Set up QEMU
        uses: docker/setup-qemu-action@v2
        with:
          platforms: arm64

      - name: Set up buildx
        uses: docker/setup-buildx-action@v2

      - name: Configure AWS Credentials
        uses: aws-actions/configure-aws-credentials@v2
        with:
          aws-access-key-id: ${{ secrets.AWS_ACCESS_KEY_ID }}
          aws-secret-access-key: ${{ secrets.AWS_SECRET_ACCESS_KEY }}
          aws-region: ${{ secrets.AWS_REGION }}

      - name: Login to Amazon ECR
        id: login-ecr
        uses: aws-actions/amazon-ecr-login@v1

      - name: Build and upload image
        uses: docker/build-push-action@v4
        with:
          context: .
          cache-from: type=gha
          cache-to: type=gha,mode=max
          file: Dockerfile.full_service
          platforms: linux/arm64 
          tags: | 
            ${{ steps.login-ecr.outputs.registry }}/code-service-dev:${{ env.IMAGE_TAG }}
            ${{ steps.login-ecr.outputs.registry }}/code-service-dev:latest
          push: true

  deploy:
    needs: [upload-docker-dev]
    runs-on: ubuntu-latest
    env:
      cache_id: deploy-dev
      flutter_version: 3.3.2
    steps:
      - uses: actions/checkout@v3

      # - uses: actions/cache@v3
      #   with:
      #     path: |
      #       ~/.cargo/
      #       ./target/
      #     key: ${{ env.cache_id }}-${{ runner.os }}-cargo-${{ hashFiles('**/Cargo.lock') }}-${{ env.RUSTC_HASH }}
      # - name: Install latest rust
      #   uses: actions-rs/toolchain@v1
      #   with:
      #       toolchain: stable
      #       override: true
      #
      - name: Configure AWS Credentials
        uses: aws-actions/configure-aws-credentials@v2
        with:
          aws-access-key-id: ${{ secrets.AWS_ACCESS_KEY_ID }}
          aws-secret-access-key: ${{ secrets.AWS_SECRET_ACCESS_KEY }}
          aws-region: ${{ secrets.AWS_REGION }}

      # - name: Add target
      #   run: rustup target add aarch64-unknown-linux-gnu
      #
      # - name: Install cross
      #   run: cargo install cross
      #
      # - name: Build target
      #   uses: actions-rs/cargo@v1
      #   with:
      #     command: xtask
      #     args: build-lambda --use-cross --target aarch64-unknown-linux-gnu

