[package]
name = "task_infra"
version = "0.1.0"
edition = "2021"

[dependencies]
aws-sdk-dynamodb = { workspace = true }
common_domain = { path = "../../common/domain" }
common_infra = { path = "../../common/infra", features = ["sqs"] }
task_domain = { path = "../../task/domain" }
serde = { workspace = true, features = ["derive"] }
serde_dynamo = { workspace = true }
lazy_static = { workspace = true }
log = { workspace = true }
